# v0.3.0 - Multi-source package search

Introduce configurable read-only package metadata sources with AUR enabled by default.

## Goals

- Load config from XDG paths and optional local config.
- Add a source abstraction.
- Support AUR RPC-compatible sources.
- Show source/repository metadata in results and details.
- Rank results across enabled sources.
